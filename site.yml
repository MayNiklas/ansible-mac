---

- hosts: localhost

  pre_tasks:
    - name: Include configuration
      include_vars: "{{ item }}"
      with_first_found:
        - files:
            - config.yml
          skip: true
      tags: ['always']

  roles:
    - role: ansible-git    
    - role: ansible-ssh
    - role: ansible-osx_defaults
    - role: ansible-homebrew
    - role: ansible-mas
